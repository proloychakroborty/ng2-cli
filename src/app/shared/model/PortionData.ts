'use strict';
import * as model from "./model"

/**
 * contains the information on portions to be registered and scheduled along with extended time (if any) for each portion.
 */
export interface PortionData {


    portionCode?: string;


    extTime?: number;
}

