'use strict';
import * as model from "./model"

/**
 * Contains the suggested geo locations data associated with requested input criteria.
 */
export interface GeoData {


    geoLocations?: Array<string>;
}

